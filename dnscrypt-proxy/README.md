## Dnscrypt-proxy
DNS traffic encryption and authentication.  

This server supports a multitude of protocols, most notably it supports DNS-over-HTTPS (DoH) over HTTP3/QUIC.  
If you prefer this over DNS-over-TLS (DoT), i recommend configuring Unbound to forward requests to this server instead.
