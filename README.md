# kira-router
A complete IPv4 and IPv6 Linux router solution based on systemd-networkd, nftables and unbound. Targeting Fedora Linux.
The NFTables is the main package, you must install it first since the other packages depends on it's common lib functions.

